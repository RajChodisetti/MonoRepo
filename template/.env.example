# Demo site template: 1 = Cinematic, 2 = Aurora, 3 = Elysian (API-only)
TEMPLATE=2

# Voice agent backend (voice-sales-agent FastAPI server)
NEXT_PUBLIC_VOICE_AGENT_URL=http://localhost:8000

# Go backend — when set, template loads OCR-classified menu/gallery images from DB
NEXT_PUBLIC_API_URL=http://localhost:8080
NEXT_PUBLIC_MEDIA_BASE_URL=

# Server-only — must match voice-sales-agent CALL_API_SECRET for POST /api/voice-agent/call
CALL_API_SECRET=
# VOICE_AGENT_URL=http://localhost:8000
