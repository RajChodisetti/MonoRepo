# Claude Code repository instructions

@AGENTS.md

Use the nearest nested `CLAUDE.md`/`AGENTS.md` for every file you edit. Run
`rtk ./scripts/agent-context.sh <planned paths>` before implementation so the
relevant dependencies and checks are loaded.
