# Google AI Studio / Gemini API key (never commit the real key)
GEMINI_API_KEY=your_gemini_api_key_here

# Optional override — default works well for vision OCR
GEMINI_MODEL=gemini-3.1-flash-lite

# Server
HOST=0.0.0.0
PORT=8090
