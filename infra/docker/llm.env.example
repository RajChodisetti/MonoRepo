# API-only public digital-footprint review. Keep production disabled here;
# `/opt/tuvi/env/llm.env` is protected mode 0600 and mounted only into the API.
LLM_PROVIDER=disabled
LLM_API_KEY=
LLM_MODEL=gpt-4.1-nano
