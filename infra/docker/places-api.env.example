# Copy to /opt/tuvi/env/places-api.env on the VM and keep mode 0600.
# This file is loaded only by the Go API so the browser never receives the key.

GOOGLE_PLACES_API_KEY=
PLACES_API_BASE_URL=https://places.googleapis.com/v1
PLACES_PHOTO_LIMIT=10
PLACES_PHOTO_MAX_WIDTH=1600
PLACES_API_TIMEOUT=20s
